/**
 * This service provides a set of helper methods around the share functionality.
 * 
 * The web version doesn't support share functionality.
 */
var ShareService = ({
	initialize: function()  {
		// nothing to do
	},
	share: function(subject, text) {
		// nothing to do
	}
});